list of entries:
    - continious list of entries mixed expenses/incomes each has a right border, income with green right border and expense with red right border. 
    - each day is clear by seperating the entries by a noticable space that's subbtle. 
    - make each odd row slightly muted bg color so it's each to read.
    - make each entry clickable so the user can view details in a seperate view.
    - each entry has the price on the right side in green if income in red if it's expense, on the left side we have the list of categories displyed in badges with there emojies on the left side,  the note uptop if exist above the categories and it's small text clamped to one line

creation of a new entry:
    - two floating buttons on the bottom right, cirlar with "+" icon inside them one on top of the other, green below red above. once clicked user is navigated to a new view, which has the price as the first field next the note and categories listed in badges in flex container below. at the bottom action buttons for cancelation and submition. each button open it's corresponding view expense and income have seperate views.
